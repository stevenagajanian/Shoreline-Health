<h3>
  <% if @user.imageurl.presence %>
  <%= image_tag @user.imageurl, class: 'img-circle', style: 'max-width: 40px; max-height: 40px;' %>
  <% end %>

  Health Record of <%= link_to @user.full_name, user_dashboard_path(@user) %>
</h3>
<hr>